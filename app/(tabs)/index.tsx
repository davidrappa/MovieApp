import { Screen } from "@/src/ui/components/Screen";

import { Text } from "@/src/ui/components/Text";

export default function PopularScreen() {
  return (
    <Screen style={{ paddingHorizontal: 0 }}>
      <Text color="white">Popular Screen</Text>
    </Screen>
  );
}
